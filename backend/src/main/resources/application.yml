server:
  port: 8015                     # 后端端口
spring:
  datasource:
    url: "jdbc:mysql://localhost:13306/works_management?useSSL=false&characterEncoding=UTF-8"
    username: root
    password: 123
    driver-class-name: com.mysql.jdbc.Driver
  jackson:
    date-format: yyyy-MM-dd       # 日期序列化格式
    time-zone: Asia/Shanghai
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.works.model
  configuration:
    map-underscore-to-camel-case: true  # 驼峰命名映射
jwt:
  secret: WORKS_MGMT_SECRET_KEY_2026
  expiration: 86400000
